<% layout ("/layouts/boilerplate")%>

<div class="row">
  <div class="col-8 offset-2">
    
    <h3 class="mb-3"><%= listing.title %></h3>

    <div class="card shadow-sm">
      <img class="card-img-top" src="<%= listing.image.url %>" alt="listing_image">

      <div class="card-body">
        <p class="card-text">
          <%= listing.description %><br>
         &#8377;<%= Number(listing.price).toLocaleString("en-IN") %><br>
          <%= listing.location %><br>
          <%= listing.country %>
        </p>

        <!-- Buttons Section -->
        <div class="mt-3 d-flex gap-2">

          <!-- Edit Button -->
          <a href="/listings/<%=listing._id%>/edit" 
             class="btn btn-primary btn-sm">
             ✏️ Edit
          </a>

          <!-- Delete Button -->
          <form method="POST" 
                action="/listings/<%=listing._id%>?_method=DELETE"
                onsubmit="return confirm('Are you sure you want to delete this listing?');">
            
            <button class="btn btn-danger btn-sm">
              🗑 Delete
            </button>
          </form>

        </div>
      </div>
    </div>

  </div>
  
  <!-- //this is my writen code -->
  <!-- <div class="col-8 offset-2">
    <h4>Leave a review</h4>
    <form>
      <div>
        <label for="rating"  class="container">Rating </label>
        <input type="range" min="1" max="5" id="
        rating" name="review[rating]"
      </div>
    <div>
      <label for="comments">Comments</label>
      <textarea name="review [comments] " 
      id="comment"
      cols="100"
      rows="10"
      ></textarea>
    </div>
    <button>Submit</button>

    </form>
  </div> -->



  <div class="col-8 offset-2 mt-4">
  <h4 class="mb-3">Leave a Review</h4>

 <form action="/listings/<%= listing.id %>/reviews" method="POST" class="needs-validation">
  
  <div class="mb-3">
    <label for="rating" class="form-label">Rating</label>
    
    <input 
      type="range" 
      class="form-range" 
      min="1" 
      max="5" 
      value="3"
      id="rating" 
      name="review[rating]"
      oninput="ratingValue.innerText = this.value"
    >

    <div class="text-muted">
      Selected Rating: <span id="ratingValue">3</span> ⭐
    </div>
  </div>

  <div class="mb-3">
    <label for="comments" class="form-label">Comments</label>
    
    <textarea 
      class="form-control"
      name="review[comment]" 
      id="comments"
      rows="5"
      required
    ></textarea>
    <div class="invalid-feedback"> please submit some comment for review</div>
  </div>

  <button class="btn btn-dark">Submit Review</button>
</form>


<!-- <h6>All Review show here</h6> -->
<ul>
  <% for(let review of listing.reviews){ %>
    <li>
      <%= review.comment %>, <%= review.rating %> stars
    </li>
  <% } %>
</ul>
</div>

</div>
<div class="col-6 offset-2 mt-4">
  <h3>Where you will be</h3>
  <div id="map"></div>
</div>
<script>
  const map = L.map('map').setView([26.8467, 80.9462], 10);

L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
    maxZoom: 19
}).addTo(map);

L.marker([26.8467, 80.9462]).addTo(map);
</script>